{
  "dependencies": {
    "firebase-admin": "^7.0.0"
  },
  "devDependencies": {
    "babel-core": "^6.26.3"
  },
  "scripts": {
    "start": "(cd client && yarn start)",
    "build": "(cd client && yarn build)",
    "serve": "firebase deploy"
  }
}
